echo-notify {
  database {
    url = "jdbc:postgresql://postgres:5432/echo_notify"
    user = "echo"
    password = "echo"
  }
  kafka {
    bootstrapServers = "kafka:9092"
    groupId = "echo-notify-consumer"
  }
  retry {
    default {
      maxAttempts = 5
      baseDelayMillis = 1000
      maxDelayMillis = 300000
    }
    byType {
      EMAIL {
        maxAttempts = 5
        baseDelayMillis = 1000
        maxDelayMillis = 300000
      }
      WEBHOOK {
        maxAttempts = 7
        baseDelayMillis = 500
        maxDelayMillis = 180000
      }
    }
  }
}
